# Copy to .env for local development with `vercel dev` — never commit .env
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/caly?retryWrites=true&w=majority
JWT_SECRET=ultraPhonkSlowedfromBr4$il

